# Serve-only dependencies for the Blackout competition server (server.py).
# Torch (CPU-only) is installed separately in the Dockerfile from the PyTorch
# CPU index to avoid pulling the CUDA toolkit.
# Training-only deps (tensorboard, matplotlib) are intentionally excluded.
numpy>=1.26,<3
fastapi
uvicorn
hisss>=1.3.0
